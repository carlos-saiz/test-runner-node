export const suma = (numA, numB) => {
    return (numA + numB)
}

export const resta = (numA, numB) => {
    return (numA - numB)
}

export const multiplicación = (numA, numB) => {
    return (numA * numB)
}

export const división = (numA, numB) => {
    return (numA / numB)
}

export const modulo = (numA, numB) => {
    return (numA % numB)
}